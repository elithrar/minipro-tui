{
  "name": "minipro-tui",
  "module": "src/main.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "dev": "bun run src/main.ts",
    "test": "bun test",
    "build": "bun build src/main.ts --target bun --compile --outfile minipro-tui --external '@opentui/core-*'"
  },
  "type": "module",
  "dependencies": {
    "@opentui/core": "^0.4.1"
  }
}
